import { JSONContent, Extension } from '@tiptap/core';

type ImportContext = {
    content: JSONContent;
    error: null;
    setEditorContent: (content?: JSONContent) => void;
    logs: {
        info: any[];
        warn: any[];
        error: any[];
    };
} | {
    content: null;
    error: Error;
};

declare module '@tiptap/core' {
    interface Commands<ReturnType> {
        importDocx: {
            /**
             * Import a DOCX file into your editor and replace the current content.
             */
            importDocx: (options: {
                file: File;
                /**
                 * The callback function to handle the imported content.
                 */
                onImport?: ((context: ImportContext) => void) | null;
            }) => ReturnType;
        };
    }
}
type ImportOptions = {
    /**
     * The Tiptap API endpoint to use for conversion.
     * @default 'https://api.tiptap.dev/v2/convert/import'
     */
    endpoint?: string;
    /**
     * The Tiptap JWT to use for authentication.
     */
    token: string;
    /**
     * The Tiptap App ID to use for authentication.
     */
    appId: string;
    /**
     * The callback URL to use for image uploads.
     */
    imageUploadCallbackUrl?: string;
    /**
     * Customize the mapping of the imported contents prosemirror types.
     * This is useful when you changed the node and mark types in your project.
     * For example instead of using `orderedList` you might use `specialOrderedList`.
     */
    prosemirrorNodes?: Record<string, string>;
    /**
     * Customize the mapping of the imported contents prosemirror marks.
     * This is useful when you changed the node and mark types in your project.
     * For example instead of using `bold` you might use `strong`.
     */
    prosemirrorMarks?: Record<string, string>;
    /**
     * The verbose level of the import, represented as a bitmask:
     * 1 = info, 2 = warn, 4 = error. You can combine levels (e.g., 3 = info + warn).
     *
     * @default 0
     */
    verbose?: number;
};
declare const ImportDocx: Extension<ImportOptions, any>;

export { type ImportContext, ImportDocx, type ImportOptions, ImportDocx as default };
